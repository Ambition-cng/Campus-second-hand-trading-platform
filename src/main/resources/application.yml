spring:
  application:
    name: Platform
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/trading_platform?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  configuration:
    map-underscore-to-camel-case: true
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.web.platform.pojo


